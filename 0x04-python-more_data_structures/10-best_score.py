#!/usr/bin/python3
def best_score(a_dictionary):
    if a-dictionary:
        for m, n in a_dictionary.items():
            if n == max(a_dictionary.values()):
                return m
            else:
                return None
