#!/usr/bin/env python3
def no_c(my_string):
    lists = list(my_string)
    for m in my_list:
        if m == 'c' or m == 'C':
            my_list.remove(m)
        my_string = ''.join(my_list)
    return my_string
